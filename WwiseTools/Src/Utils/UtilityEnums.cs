namespace WwiseTools.Utils
{
    public enum ImportAction
    {
        useExisting,
        replaceExisting,
        createNew
    }
    
    public enum NameConflictBehaviour
    {
        fail,
        replace,
        merge,
        rename
    } 
}

